# ربط كشف الطلاب ودرجات الواجبات

## التحقق من الطلاب

يقرأ الموقع رابط النشر العام الموجود في `server/roster.ts` بصيغة CSV. يجب أن يحتوي الصف الأول في الورقة على عناوين واضحة مثل:

| الاسم | رقم الكشف | الدرجة |
|---|---|---|
| أحمد رضا | 12345 | |

يكفي أن تكون عناوين الاسم والرقم متضمنة كلمتي «اسم» و«رقم» أو «كشف» أو «هاتف». القراءة تتم من الخادم، ويُرفض الوصول إذا كان الكشف غير متاح أو لم تتطابق القيمتان.

## تصحيح الواجبات

التصحيح يتم آليًا على الخادم بواسطة Gemini 2.5 Flash عند ضبط `GEMINI_API_KEY`. لا يحتاج المدرّس إلى اعتماد الدرجة؛ لوحة المدرّس تعرض النتائج للمتابعة فقط.

## حفظ الدرجات

رابط `pubhtml` المنشور للقراءة فقط ولا يمكنه تعديل الورقة. لحفظ الدرجات، أنشئ Google Apps Script مرتبطًا بالملف الأصلي، وضع Web App بصلاحية مناسبة، ثم خزّن رابط Web App في الإعداد الخادمي `GOOGLE_SHEETS_UPDATE_WEBHOOK_URL`. يجب أن يستقبل الـ Web App الحقول `studentName`, `phone`, `grade`, `assignmentTitle`, `score`, و`maxScore` ثم يضيفها أو يحدّثها في ورقة الدرجات. استخدم رمزًا مشتركًا في `GOOGLE_SHEETS_UPDATE_TOKEN` إذا كان Web App لا يملك حماية أخرى.

لا تضع رابط التعديل الأصلي أو بيانات دخول Google داخل الواجهة. رابط النشر العام يُستخدم للتحقق فقط، أما الكتابة فتحتاج Web App خادميًا مستقلًا.
